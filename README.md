# Telephone-Diary

Built in C

Uses Bubble Sort for alphabetical ordering

Uses Binary Search for fast contact lookup

Supports Add, Update, Delete, Display, Search

Demonstrates practical use of arrays, structs, sorting, and searching
